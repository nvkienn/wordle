from solver_code import entropy, all_entropy,first_guesses,best_entropy,worst_entropy
from base_game_code import colorize
import listw
a = all_entropy(listw.ans)
for i in range (10):
    print (a[i])
